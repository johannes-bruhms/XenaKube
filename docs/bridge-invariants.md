# Bridge Invariants — Full Enforcement Detail

> CLAUDE.md keeps a compressed summary of each invariant (claim + where + failure log). This file is the source of truth for *how* each invariant is enforced and *what* the failure logs surface. Read this file before changing any code that touches an invariant.

SWAM Cello is a stateful physical-modeling VST. Several features (portamento, harmonics, tremolo, bow polyphony, expression envelope) depend on the plugin being in a specific internal mode that no MIDI wire inspection can confirm. These are silent-failure surfaces: you can emit "correct-looking" MIDI and get no slide, no harmonic, no tremolo. Every such feature must be paired with a runtime invariant that fails loudly.

**Hard rule for modulators** (face envelope, intensity map, regime multipliers, path scalars, voice-stealing, any future feature that touches voice dispatch): they may *reshape* a complex's gesture (duration, velocity, density, register, contour) but MUST NOT *eliminate* the structure that defines the complex's identity. Face `isSingle` collapsing C5/C6/C7 to zero gliss steps was the exact mistake D42 fixed — don't repeat the pattern with any other complex/mode combination.

When you add a new modulator that intersects an existing invariant, re-audit every invariant in the list below. "Assumed preserved" is how regressions happen.

| Invariant | Scope | Enforcement | Telemetry on failure |
|-----------|-------|-------------|----------------------|
| **Gliss event count** (D42 + D46 + D59) — every C5/C6/C7 voice emits ≥1 `glissStep` call. `glissStep` classifies the transition as a legacy same-string portamento slide (`glissNote`), pitchbend slide (`bendStep`), or over-range/string-crossing fallback (`leapStep`) and tallies into `glissOverlapCount` / `glissBendCount` / `glissLeapCount`. Invariant is `slides + bends + leaps ≥ 1`, not `slides ≥ 1`. | `max/xk_swam.js` `glissStep` (dispatch) + `sameString` + `CELLO_STRINGS` table (string ranges) + `phraseC5` / `phraseC6` / `phraseC7` (route every gliss pitch through `glissStep`). | Structural: phrases call `glissStep` directly. `faceShapedCount(forGliss=true)` minimum is 1, not 0. `phraseC6` forces `count ≥ 2`. `phraseC7` always fires ≥1 drift regardless of `isSingle`. | Always-on per-phrase log line: `inst N CX face=F slides=S bends=B leaps=L dur=D` from `scheduleRelease`'s natural-end task. Promotes to `GLISS FAIL ... slides=0 bends=0 leaps=0 ...` if all three are 0. Stolen phrases clear `glissExpected` in `stealInstance` to avoid false positives. The slide/bend/leap breakdown lets the user audit gliss distribution per face/intensity in real time. |
| **Immediate first gliss** (D43 + D53) — the first `glissStep` of every C5/C6/C7 voice fires at a per-complex first-gliss time from phrase start, independent of duration. C5/C6 use `FIRST_GLISS_MS = 150` ms (anchor establishes a clear starting pitch before the slide). C7 uses `FIRST_GLISS_MS_C7 = 30` ms (D53 — anchor barely audible, drift starts almost immediately so C7's "wispy ephemeral wandering" character reads as continuous floating rather than C6's deliberate stepping). A stolen-short voice (fast turn at ~300 ms) still delivers an audible slide instead of playing as a sustained single note. | `max/xk_swam.js` `FIRST_GLISS_MS` and `FIRST_GLISS_MS_C7` constants; `phraseC5` / `phraseC6` / `phraseC7` schedulers | Structural: phrases schedule idx 0 (C5, C7) or idx 1 (C6) at the per-complex first-gliss time as a hard constant; remaining slides distribute through the phrase tail. `scheduleRelease`'s 200 ms floor is always > both first-gliss times, so the first slide never races the release ramp even at tiny durations. C7's 30 ms is also large enough that the noteon→scheduled-noteoff window remains GLISS_OVERLAP_MS = 60 ms (the overlap is the schedule, not the gap between the two noteons). | No dedicated failure log — the D42 "GLISS FAIL overlaps=0" counter catches any regression that would affect this too. Slide latency is a tunable, not an invariant. |
| **Turn-to-noteon latency telemetry** — every `/xk/voice` emitted by the engine should produce a first non-companion `/xk/midi/noteon` echo quickly enough for live play. This probe measures relay→Max bridge timing only: timestamp at `engine.onVoice`, then timestamp the first non-companion MIDI noteon echo returning over UDP 57122. It does **not** measure SWAM acoustic attack, `Auto Poly Detect Time`, host audio-buffer latency, or slow CC 11 ramps that can make a timely note feel late. Those must be diagnosed after the bridge echo budget is proven. | `relay.js` computes the `PhrasePlanner` plan, then `startVoiceLatencyProbe(output, phrasePlans)` stores planned first-onset/count metadata before sending OSC to Max; `completeVoiceLatencyProbe(data)` consumes the oldest pending probe on first non-companion `OSC.MIDI_NOTEON`; `expireVoiceLatencyProbes(now)` fails probes with no echo. Per-complex rolling windows are kept in `latencyByComplex`. The phrase echo auditor below is the stricter plan-id-aware version; this probe remains as the coarse human-readable latency window. | Structural: one pending probe per `engine.onVoice` output. Companion noteons are ignored so C5/C6 double-stop companions cannot satisfy the first-onset probe. Pending probes are capped and expired so a missing Max echo cannot poison later measurements indefinitely. | `[LATENCY WARN] ...` at `LATENCY_WARN_MS = 150`; `[LATENCY FAIL] ...` at `LATENCY_FAIL_MS = 250`; missing echoes fail after 3000 ms. Logs include `planFirst`, `planOverrun`, planned note/bend/companion counts, per-complex rolling `n`, `p50`, `p95`, and `max`; every eighth normal sample logs the same summary. |
| **Immediate expression seed** — non-arc phrase envelopes must not make the first note wait for CC 11 to climb from zero. Before this invariant, `scheduleExprEnvelope` ramped from the prior value to the attack value over `max(baseAttack, min(durMs * 0.10, 800))`; with 2-5 s vertex durations that produced 200-500 ms of quiet onset, and with face duration bias it could hit the 800 ms cap. That sounded exactly like per-face latency or dropped notes even though MIDI noteon was timely. | `max/xk_swam.js` `scheduleExprEnvelope` now `ccForce`s `peakExpr * env.attack` synchronously before phrase dispatch, then schedules the later peak/sustain ramps. Phrase arcs and hairpins already seeded their start value synchronously via `schedulePhraseArc` / `schedulePhraseHairpin`. Relay tracks latest `/xk/midi/expr` echo per voice and annotates the first-noteon latency log with `expr=V exprAge=Nms`. | Structural: the attack CC write happens before the phrase generator switch in `handleVoice`, so every immediate `noteOn` / `legatoNote` sees an already-seeded CC 11. The later peak/sustain timing remains phrase-shaped; only the silent attack ramp from zero is removed. | `[ONSET EXPR WARN] first noteon arrived quickly but CC11 is low ...` when bridge noteon latency is under the warn threshold but the latest expression echo for that voice is below 32. This is diagnostic, not a hard failure: very soft K-dynamics can be intentionally below 32, but the log separates "MIDI was late" from "MIDI was on time but acoustic energy was low." |
| **Material duration + face multiplier** (D74) - each voice resolves phrase duration from K_i base duration, optional `FACE_MAP.durationMult`, a per-complex floor, and a 30 s cap. This preserves the old long/short material while keeping face gestures temporally recognisable. | `src/vertices.ts` V1 `U` durations; `src/face-gesture.ts` `durationMult`; `src/swam-mapping.ts` `resolvePhraseDuration` / `COMPLEX_DURATION_FLOOR_SEC`; generated `max/gen_includes.js`; `max/xk_swam.js` `handleFace` / `handleVoice`; `src/osc-output.ts` sends `/xk/face -` on null faces to prevent stale-face multiplier bleed. | Structural: normal face turns set `state.faceDurationMult` from generated FACE_MAP; `handleVoice` multiplies the incoming vertex duration, applies `durationFloorForComplex`, and clamps to `MAX_PHRASE_DURATION_SEC`. TS phrase plans use the same resolver. | `FACE DURATION MULT FAIL face=F ...` if FACE_MAP lacks `durationMult`; normal voice logs include `dur=Ns(vertex*face...)`; tests in `test/swam-mapping.test.ts` and `test/phrase-plan.test.ts`. |
| **Bounded turn-rate pressure** - turn rate may add dramatic pressure, but it must not become a hidden second K-cube or erase complex identity. The scalar is `turnRatePressure = clamp((turnRate - 0.3) / (3.0 - 0.3), 0, 1)`. Per-complex `RATE_*` gains can raise density high bounds, note velocity, expression peak, bow pressure, C8 tremolo steady rate, and C5 bow-pressure accent. C8 density gain stays 0; C6/C7 density gains stay small; C5's `WILD_MIN_COUNT` floor is unchanged. | `src/swam-mapping.ts` owns `RATE_PRESSURE_*`, `RATE_*_GAIN_BY_COMPLEX`, pure multiplier helpers, and `phraseCountBounds`; `scripts/gen-max-include.js` emits the tables; `src/phrase-plan.ts` applies the same helpers as the legacy renderer; `max/xk_swam.js` mirrors helper names and uses generated constants at v8 load. | Structural: TypeScript and Max both derive from the same generated tables. `phraseCountBounds` only multiplies the high bound, preserving low/floor behavior. C1/C4 event rates and C7 non-single drift count use the rate multiplier directly; `handleVoice` applies velocity/expression/bow/tremolo multipliers once at voice onset; C5 accent uses `rateAccentValue` without touching slide velocity. | Max voice logs include `rateP=N.NN` for every `/xk/voice`, so live pressure is visible in `[print xk_swam]`. Tests in `test/swam-mapping.test.ts` assert bounded pressure, table coverage, C8 density inertness, and C5-only accent growth; `test/phrase-plan.test.ts` asserts fast C1 plans get denser and fast C2 expression rises. |
| **Shadow phrase plan + echo audit seam** - every voice transition now has a relay-side TypeScript plan for intended C1-C8 phrase structure before the Max legacy renderer runs, and every Max MIDI echo is stamped with that plan id. The plan is not yet the audio executor; it is the migration invariant that makes the Max/dashboard split measurable instead of implicit. | `src/phrase-plan.ts` `PhrasePlanner`; `src/phrase-audit.ts` `PhraseEchoAuditor`; `relay.js` computes plans inside `engine.onVoice`, starts the auditor, broadcasts WS `phrase_plan` / `phrase_audit`, and sends compact `/xk/phrase/plan` summaries immediately before each `/xk/voice`; `max/xk_swam.js` consumes `state.currentPlanId` once per voice and appends it to `/xk/midi/noteon`, `/xk/midi/noteoff`, `/xk/midi/bendstep`, and `/xk/midi/expr`; dashboard displays phrase plan and phrase audit rows. | Structural: tests assert K-duration × face-multiplier planning, complex duration floors, near-immediate first planned note for all complexes, C7 no companions, C5 companion planning, missing-first-note failure, stolen-before-sound failure, missing-gliss-bend failure, and unexpected-C7-companion failure. Exact stochastic count drift is a warning until TypeScript owns phrase execution because Max still rolls independent randomness. | `[PHRASE ECHO FAIL] P...` for missing/late first noteon, all-missing planned bendsteps, or unexpected companions; `[PHRASE ECHO STOLEN]` when a phrase is preempted; `[PHRASE ECHO OK]` for completed matches or warning-only stochastic drift. Dashboard state row `phrase audit`; Max log `phrasePlan P... C...`; tests in `test/phrase-plan.test.ts` and `test/phrase-audit.test.ts`. |
| **Min gliss spacing** (D45) — consecutive `glissStep` events on a single phrase are never closer than `MIN_GLISS_SPACING_MS` (200 ms). Current C5/C6/C7 normally route through `bendStep`, so the floor gives each pitchbend ramp and atomic noteoff/noteon transition room to complete before the next target. Legacy `glissNote` portamento fallback also needs roughly this much time to engage. Phrases that can't fit their requested count at this spacing get `count` clipped to whatever fits. | `max/xk_swam.js` `MIN_GLISS_SPACING_MS` constant; `glissSchedule` helper used by `phraseC5` / `phraseC6` / `phraseC7` | Structural: every gliss complex routes its event timing through `glissSchedule(maxCount, firstMs, tailEnd, MIN_GLISS_SPACING_MS)` which enforces the floor and truncates count if events would run past `tailEnd`. | No dedicated failure log — same shared D42 "GLISS FAIL" catches anything that breaks gliss entirely. Spacing is a tunable; if SWAM/Max scheduling behaviour changes, raise/lower `MIN_GLISS_SPACING_MS`. |
| **Leap-alternation with per-complex tolerance** (D48 + D58) — leaps (cross-string `leapStep`) are capped per gliss complex via `MAX_LEAPS_BY_COMPLEX = { 5: 1, 6: 0, 7: 0 }`. Once a complex's count is reached, the next would-be cross-string outcome is nudged onto source's strings to force a slide. **C5 (wild gliss)** keeps `maxLeaps = 1`: occasional cross-string punch is part of the wild character; `phraseC5` seeds `inst.consecutiveLeapCurrent = MAX_CONSECUTIVE_LEAPS` at phrase start so the first event is forced to slide ("wild gliss must always begin with a slide"). **C6 (ord. gliss)** and **C7 (tasto)** use `maxLeaps = 0`: every cross-string outcome is nudged to same-string. **Now mostly inert** because D59 (cross-string pitchbend slides) routes most cross-string outcomes within `PITCHBEND_RANGE_SEMI` to `bendStep` (counts as a slide for the leap counter); the per-complex max only fires for over-range intervals (rare). Table preserved as a belt-and-suspenders fallback if pitchbend is ever disabled (e.g. SWAM preset reset to ±2). | `max/xk_swam.js` `MAX_LEAPS_BY_COMPLEX` table + `maxLeapsFor(complexType)` helper; `glissStep` (counter-gated nudge using per-complex max) + `nudgeToSameString(sourcePitch, preferTarget, minLeap)` (picks a same-string target ≥ minLeap from source; candidate strings shuffled when source sits in a multi-string overlap zone so trajectory isn't biased toward one string) + `phraseC5` seeds `inst.consecutiveLeapCurrent = MAX_CONSECUTIVE_LEAPS` for the forced-slide opening; `handleVoice` zeros `consecutiveLeap*` counters generically. Per-complex max is the single tunable; raise C5 to 2 if the wild punch feels too tame, or set every entry to 0 to eliminate cross-string leaps entirely. | Structural: `glissStep` checks `(inst.consecutiveLeapCurrent | 0) >= maxLeapsFor(inst.activeComplex) && !sameString(...)` and nudges target to same-string before dispatch. `nudgeToSameString` returns null only when source is at the extreme of every string it sits on with no minLeap headroom in either direction (rare — extreme cello-range corners). | Per-phrase log in `scheduleRelease` natural-end task tracks `inst.consecutiveLeapMax`. `consecutiveLeapMax > maxLeapsFor(inst.activeComplex)` (run exceeded per-complex tolerance despite the nudge) promotes to `GLISS RUN FAIL inst N CX face=F slides=S leaps=L consecLeapMax=M (max for CX = M_max) dur=D`. Otherwise the existing per-phrase `slides=S leaps=L` line is unchanged. For C6/C7 (max=0) ANY leap trips the FAIL — should be rare-to-never; a hit indicates a `nudgeToSameString` corner failure or a regression. |
| **Selector re-assertion** (D44 + D69) — CC 78 (Harmonics) / CC 79 (Tremolo) / CC 81 (Bow Polyphony) are written by `setHarmonics` / `setTremolo` / `setBowPolyphony` regardless of cached state, and CC 81 is additionally re-asserted in `handleVoice` per `/xk/voice` event. Selectors can desync between the bridge and SWAM (preset reload races, MIDI Learn glitches, voice steal during plugin re-init). Diff guards that "skip the same value" silently freeze the plugin in the wrong mode — D44 was Mono Poly Release stuck on (killing double stops); D69 was tremolo stuck on at startup (every C1–C7 phrase tremolo'd until the first C8 broke the cache `OFF→FAST→OFF`). The race is identical in both cases: `resetInstance` writes its CC before SWAM's preset finishes loading, so the bridge cache reads "off" while SWAM lands on whatever the preset / `vst~` default is, and every later same-value call returns early through the diff guard. `setHarmonics` got the same fix prophylactically — the C4-only callsite hadn't been reported broken yet, but it carries the same race. | `max/xk_swam.js` `setHarmonics` / `setTremolo` / `setBowPolyphony` (no diff guard) + `handleVoice` per-voice CC 81 re-assert. `setupComplex` calls all three on every complex switch. | Structural: each function always writes its CC; `handleVoice` writes CC 81 again per voice using `cmx.bowPoly` from the COMPLEX table. | `log("inst N harmonics=H cc78=V")` / `log("inst N tremolo=T cc79=V")` / `log("inst N bowPoly=B cc81=V")` on every selector write — read the Max console to verify the bridge is asserting the expected mode for each complex. The D44 setBowPolyphony comment block in `xk_swam.js` is the canonical race description; the D69 setTremolo block is the same race observed on a second selector. |
| **Phrase dynamic arc** (D47, D57) — sustained-and-gliss multi-note complexes (C3/C4/C5/C6/C7/C8) emit a single linear CC 11 ramp across the full phrase duration: cresc TO `inst.peakExpr × ARC_CEIL` for swell faces (L/F/F'), dim FROM `inst.peakExpr × ARC_CEIL` to `× ARC_FLOOR` for fade/burst faces (U'/L'/R'). Hairpin faces (D' = `<>` hairpin-up, B' = `><` hairpin-down) route to `schedulePhraseHairpin` instead — start → mid → start trajectory over `durMs/2` + `durMs/2` halves. **C1 pizz (D57)** uses static `ccForce(CC.EXPRESSION, peakExpr)` at phrase start with NO envelope — per-pluck velocity is the dynamic that audibly belongs to pizz. **C2 directional run (2026-05-07)** is excluded from `ARC_COMPLEXES`: the phrase arc is realized PER-NOTE inside `phraseC2` (velocity, CC 11 (Expression), and `inst.bowPosBase` migration are shaped per-stroke from `phraseArcDirection(inst)` — cresc/dim/hairpin direction comes from face envelope, swing depth is 0.55→1.0). CC 17 (Bow Pressure) is sampled once per note from `inst.bowPressureBase` ± `C2_BOW_PRESSURE_JITTER` and held static for that note. **C2 baseline is `BOW_POLY.MONO_POLY_RELEASE`** (not `DOUBLE_HOLD` like other complexes), and `phraseC2` flips CC 81 PER NOTE: monophonic notes write `MONO_POLY_RELEASE` (SWAM auto-releases the held note on the next noteOn → smooth legato, no chord), intentional double-stop notes (probability `C2_DOUBLE_STOP_PROB = 0.30`, first note always solo) write `DOUBLE_HOLD` so the main + companion (picked via `doubleStopCompanion`) sustain together as a true held dyad. Race-free: each note's task writes its own CC 81 before any noteOns, so mode is correct at every noteOn time regardless of when the previous note's scheduled noteOff fires. Double-stop notes have `noteDurMs` capped at `spacingToNext - C2_DOUBLE_STOP_GUARD_MS = spacingToNext − 5` so the dyad's noteOffs fire before the next note's noteOn — preventing 3-voice texture from release-tail bleed in DOUBLE_HOLD. Monophonic notes have `noteDurMs` capped at `spacingToNext` (no overlap, MONO_POLY_RELEASE handles the smooth transition). Result: monophonic transitions can never chord (structural via SWAM mode); double stops are heard as held dyads exactly when intended. Static guard in `test/max-bridge.test.ts` asserts the baseline mode AND that `phraseC2` writes both modes via `CC.BOW_POLYPHONY` AND uses `doubleStopCompanion`. Inside `phraseC2`, per-note absolute schedule times are computed up-front (`noteOnAbs[i] = noteTimes[i] + humanDelay()`) and `noteDurMs[i] = min(ringMs[i], spacingToNext)` for monophonic notes or `spacingToNext - C2_DOUBLE_STOP_GUARD_MS` for dyads — the cap is humanDelay-aware, keeping bow-polyphony and bow-pressure changes aligned to note boundaries. A phrase-wide CC 11 ramp under detache notes made C2 audibly indistinguishable from C3 (constant-register cloud), erasing C2's directional-run identity; per-note shaping keeps C2 as stroked scale material without a phrase-wide swell underneath; bow pressure stays a discrete value per note rather than a timbral ramp inside the stroke. The `handleVoice` dispatch short-circuits C2 to a synchronous CC 11 seed only — no scheduled peak/sustain ramps fight the per-note `ccForce` writes inside `phraseC2`. **C2 CC 11 floor** (`C2_MIN_PEAK_EXPR = 46`): applied as a minimum on `inst.peakExpr` for C2 only, immediately after the standard `peakExpr = intMap.expr × envPeakMult × rateExpressionMultiplier` computation in `handleVoice`. The floor exists because `arcMul`'s soft endpoint is `0.55 × peakExpr`, and at low K-intensities (ppp peakExpr=15, pp=31) that natural soft endpoint dips below 24 — too low to read as a clear scale-run note in the texture. Peak expression of 46 → soft endpoint = 25.3 (rounds to 25, always > 24). Mid/high K-intensities (mp peakExpr=63, mf=79, ff=111) pass through unchanged. The 0.55→1.0 swing is preserved at every intensity; "scaling everything else accordingly" means at low K-intensities both floor AND ceiling rise (pp peakExpr 31 → 46, range 25→46) so the per-note arc shape stays intact. Mirror in `src/phrase-plan.ts` for audit accuracy. **Within-phrase tempo curve** (`buildC2Tempo`): each C2 phrase has accelerando / ritardando / accel-rit / rit-accel motion coupled to arc direction (cresc→accel, dim→rit, hairpin-up→accel-rit, hairpin-down→rit-accel, null-arc→random per phrase). Curve is **exponential in rate** (geometric) using compressed progress `w = min(1, u / C2_CURVE_END_U)`: accel/rit/hairpin motion reaches its endpoint by phrase midpoint, then holds terminal rate. Endpoints scale with `turnRatePressure()` ∈ [0,1]: at turnP=0, range `[C2_RATE_MIN=4, 8]` notes/sec; at turnP=1, range `[6, C2_RATE_MAX=12]` notes/sec. Span ratio is held constant at `C2_RATE_SPAN_RATIO=2` across all turn rates so the within-phrase swing is always 2x - audible without the prior steep density surge. Replaces the previous baseRate × tempo-multiplier model: turn-rate scaling now lives entirely in the endpoints, not in a separate `rateDensityMultiplier(2)` layer (single source of truth). Implementation: trapezoidal-integrate the tempo curve into a 100-sample cumulative phase array, place notes at phase-uniform targets via binary search so local note rate matches local `tempoCurve(u)` exactly. **Emergent articulation** (no regime switch — replaces the prior detache-vs-legato hybrid): every C2 note rolls a random "intrinsic ring time" `r ~ uniform[C2_RING_MIN_MS=120, C2_RING_MAX_MS=320]`. Actual scheduled noteOff = `min(r, localSpacing)` for monophonic notes and slightly before `localSpacing` for intentional dyads. C2's baseline `bowPoly = MONO_POLY_RELEASE` keeps ordinary transitions from chording; double-stop notes temporarily switch to `DOUBLE_HOLD` for the dyad. Result: `r ≥ localSpacing` (always true at fast tempi where spacing < 120 ms) → noteOff lands at the next note boundary → legato; `r < localSpacing` (only possible at slow tempi where spacing > 120 ms) → noteOff lands before next noteOn → detache emerges with `localSpacing − r` gap. Mirrors how real cello playing produces detache only when bow movement outpaces finger placement, not as an explicit articulation switch. isSingle face envelopes (pluck/stab/hairpin-up/hairpin-down) on non-pizz, non-C2 complexes resolve to one note; pluck/stab fall through to `scheduleExprEnvelope`, which now seeds attack synchronously and only ramps later peak/sustain stages. **Chaining** (cresc/dim only, not hairpin which is symmetric/self-contained): consecutive same-direction voices arriving within `ARC_CHAIN_GAP_MS = 1000` ms of the previous arc's exit (natural-end OR steal) CHAIN — the new arc inherits `ccCache[CC.EXPRESSION]` (no ccForce snap) so a long swell across multiple cube turns reads as one continuous dynamic. Direction mismatch / timeout / first arc breaks the chain cleanly. C2 does not chain (each detache run is self-contained). | `max/xk_swam.js` `ARC_FLOOR` / `ARC_CEIL` / `ARC_COMPLEXES = {3,4,5,6,7,8}` / `ARC_CHAIN_GAP_MS` constants; `phraseArcDirection(inst)` (face envelope → 'cresc'/'dim'/'hairpin-up'/'hairpin-down'/null) + `schedulePhraseArc(inst, peakExpr, dir, durMs)` (cresc/dim, with chain check via `inst.lastArcDir/lastArcEndTime`; ccForce snap only when chain breaks; single rampCC) + `schedulePhraseHairpin(inst, peakExpr, dir, durMs)` (hairpin-up/down, two rampCC halves with mid peak/trough at `durMs/2`); dispatch in `handleVoice` selects arc vs hairpin vs C1-static vs C2-seed vs `scheduleExprEnvelope` per `(complexType, faceEnvelope)`. C2's per-note arc lives in `phraseC2`'s `arcMul(idx, total)` helper; `bowPressureVals` samples one held CC 17 value per note. Voice steal AND natural-end refresh `inst.lastArc*` so subsequent voices can chain off either termination. | Schedule-time `inst N phraseArc dir=cresc face=swell start=38 end=127 dur=2500ms` log confirms direction landed before play; appended `[chained gap=420ms]` when chain fired. Hairpin emits `inst N phraseHairpin dir=hairpin-up face=hairpin-up start=38 mid=127 end=38 dur=2500ms`. Natural-end assertion in `scheduleRelease`: `ccCache[CC.EXPRESSION]` should equal `phraseArcEnd` ±8 of 127 (~6%); promotes to `ARC FAIL inst N CX face=F dir=D landed=L want=W off=O dur=D` on miss (C2 is exempt — `inst.phraseArcDir` is set to null). For hairpin, `phraseArcEnd === phraseArcStart` (returns to start) so the fade picks up at low (hairpin-up) or high (hairpin-down) — the latter has the longer audible release, an intentional asymmetry. Hits emit `inst N CX arc=cresc 38->127 dur=2.50` for direction audit. C2 emits its own per-phrase line `[phraseC2 RUN] inst=N count=N tempo=... rate=Lo→Hi n/s spacing=Smax→Smin ms detache=D/N doubles=D/N arcDir=... turnP=N.NN dur=Ds` so a future refactor that re-collapses C2 to "long tones" (sparse count, pure legato, double stops back) is visible immediately. Chain candidacy is refreshed in BOTH `scheduleRelease`'s natural-end task AND `cancelPhrase` (steal path) so a stolen mid-arc voice still seeds a chain candidate for the next voice. |
| **C3 within-note bow motion** - each C3 held note must begin bow-colour motion at its noteOn, not wait for a later gyro packet or phrase boundary. The note samples the current CC 11 from `ccCache[CC.EXPRESSION]` at onset. Lower sampled CC 11 yields a higher relative speed, so soft C3 notes travel through bow position/pressure faster while loud notes shift more slowly. Both CC 16 (Bow Position) and CC 17 (Bow Pressure) ramp from the current cached value toward a nearby endpoint; CC 16 endpoints stay inside `BOW_POSITION_MIN..BOW_POSITION_MAX` (`0..64`), and no `ccForce` jump is inserted unless the cache is unseeded. | `max/xk_swam.js` `phraseC3` precomputes note times, samples CC 11 inside the same scheduled task that fires `legatoNote`, and calls `scheduleC3BowMotion(inst, noteDurMs, exprAtOn)` before noteOn. `scheduleC3BowMotion` computes speed from CC 11, calls `c3ShiftEndpoint(posStart, posMag, BOW_POSITION_MIN, BOW_POSITION_MAX)` for CC 16, and drives `rampCC` on CC 16/17. `handleExprTilt` and `handleExprScramble` skip `activeComplex === 3` so the global bow-position stream cannot overwrite C3's per-note ramp. `cancelPhrase` cancels CC 16/17 ramps on steal. Static guard in `test/max-bridge.test.ts` asserts the sampling, ramp calls, lower-half endpoint bounds, and C3 skips. | `scheduleRelease` asserts `inst.c3BowMotionCount >= 1` for natural C3 ends. Failure logs `C3 BOW MOTION FAIL inst N face=F dur=D`; normal C3 phrases log `inst N C3 bowMotion count=N minExpr=E maxRate=R dur=D`, which proves the onset sampler saw CC 11 and the inverse-rate mapping was active. |
| **Bow-position smoothness / lower-half guard** (D54) - `handleExprTilt` writes CC 16 (Bow Position) at 30 Hz from gyro tilt, EMA-smoothed to match the `spinEMA` / `devEMA` convention. Without smoothing, gyro static-pose noise (~0.005 in normalised tilt) amplified through `(val - 0.5) * 60` and `Math.round()` flickers between two adjacent CC integers, which SWAM hears as a 30 Hz bow-position wobble on sustained voices. Every CC 16 writer is also constrained to the lower half of the MIDI range (`0..64`). | `max/xk_swam.js` `state.tiltEMA` (initialised at rest pose 0.5); `TILT_EMA_ALPHA = 0.05` constant (smaller than `spinEMA`'s 0.08 because tilt is always-emitting with no deadband); `handleExprTilt` updates `tiltEMA`, computes jitter from it, and writes `newVal = clampBowPosition(...)`. `cc`, `ccForce`, and `rampCC` route `num === CC.BOW_POSITION` through `clampBowPosition`; `setupComplex` clamps per-complex baselines; C3/C4 bow-motion helpers generate endpoints inside `BOW_POSITION_MIN..BOW_POSITION_MAX`. Per-instance `bowPosWrites` / `bowPosReversals` / `bowPosLastDir` counters track CC 16 emit pattern within a voice. | Per-phrase log `inst N bowPosFlap rev=R writes=W rate=R/s` always emits when CC 16 fired during the voice; promotes to `BOW POS FLAP inst N CX reversals=R writes=W dur=D (rate=R/s, fail>BOW_FLAP_RATE_FAIL)` when reversals/sec exceeds `BOW_FLAP_RATE_FAIL = 10`. Static guard in `test/max-bridge.test.ts` rejects CC 16 writers that bypass the lower-half clamp. Counters reset in `handleVoice` and `bang`'s reset block. The threshold tolerates active tilting (~5-10 reversals/sec is legitimate user gesture); flap regression returns 30+ reversals/sec on a held cube. |
| **Portamento state at slide** (D55, diagnostic) — every `glissNote` invocation (the slide noteOn for same-string gliss steps) MUST be backed by SWAM having `CC 5 (PORTAMENTO_TIME) = cmx.portamento.time`, `CC 64 (PORTAMENTO_ON) = 127`, and (if `HAS_BOW_POLY_CC`) `CC 81 (BOW_POLYPHONY) = BOW_POLY_CC_VAL[cmx.bowPoly]`. The "wiggle through 0" trick in `setupComplex` / `handleVoice` (CC 5 = 0 → CC 5 = time, forcing SWAM v3 to register a parameter-change event) can race a noteOn if the two writes straddle a MIDI buffer boundary; SWAM ends up latched at 0 → instant glide → no audible slide while the bridge still emits noteon-overlap and the dashboard draws as gliss. Diagnostic-only for now; logs identify whether bridge `ccCache` agrees with intent. NOT a fix for SWAM↔bridge wire drift (would need a `vst~ pattr` read-back, deferred). | `max/xk_swam.js` `glissNote` entry-time assertion. Reads `inst.ccCache[CC.PORTAMENTO_TIME]`, `inst.ccCache[CC.PORTAMENTO_ON]`, `inst.ccCache[CC.BOW_POLYPHONY]` against `cmx.portamento.{on,time}` + `BOW_POLY_CC_VAL[cmx.bowPoly]`. | `PORT TIME FAIL inst N CX pitch=P wantTime=W gotTime=G gotOn=O polyOk=B` on any mismatch. If this fires under load, the next fix is the wiggle-without-zero pattern: write `time + 1` then `time` so a racing noteOn at worst sees a 1-unit-slower slide (graceful degradation), never silent failure. Always-on; cost is one cache lookup per slide noteOn. |
| **Cross-string slide via pitchbend** (D59 + D72) — `glissStep` three-way dispatch routes every gliss step into one of three paths based on `(interval, sameString, cmx.slideViaBend)`: (1) `|interval| > PITCHBEND_RANGE_SEMI` → `leapStep` (with `BEND CLIP` log; over-range fallback). (2) `|interval| ≤ PITCHBEND_RANGE_SEMI` AND (`cmx.slideViaBend === true` OR `!sameString(src, dst)`) → `bendStep` (pitchbend wheel ramp on the held source, then atomic `bend=0 + noteOff(source) + noteOn(target)` at ramp end). (3) Otherwise → `glissNote` (legacy same-string portamento fallback). Current `COMPLEX[5]`, `[6]`, and `[7]` all set `slideViaBend = true`, so normal gliss steps route through `bendStep` and can scale each slide to the gap-to-next-event, bypassing the CC 5 ≤ 127 ms/semi portamento cap. Bend source must be looked up in `inst.activeNotes`, NOT re-derived via `humanPitch` (which has a 10%-chance ±1 semi shift; re-call would mismatch noteOff). When `desiredDurMs` is null, bend duration falls back to `_bendDur(from, to, complex)` clamped to `MIN_GLISS_SPACING_MS - BEND_DUR_MARGIN_MS = 195 ms` for D60 race-safety. | `max/xk_swam.js` `bendStep(inst, src, dst, vel, accent, complex, desiredDurMs)` finds `heldSource` via `Math.round`-tolerant lookup in `inst.activeNotes`; `emitPitchbend` / `rampPitchbend` (5 ms tick) / `cancelPitchbendRamp` helpers; `inst.pitchbend` / `inst.pitchbendRampTasks` / `inst.bendPending` / `inst.bendPendingTask` instance state. `completeBend(inst, scheduled)` is the single execution path for the atomic ramp-end transition — called either from the scheduled task at `t+durMs` OR force-inline from `glissStep` entry when `inst.bendPending` is set (race-safety against scheduling jitter < 5 ms). `glissStep(inst, src, dst, minLeap, vel, accent, bendDurOverride)` forwards `bendDurOverride` to `bendStep` as `desiredDurMs`. `phraseC5`, `phraseC6`, and `phraseC7` compute per-slide/per-drift `bendDur = clamp(gapMs - 50, 80, MAX_BEND_DUR_MS)` from the time to the next event or phrase end. `cancelPhrase` + `bang` reset pitchbend to `PITCHBEND_CENTER = 8192` AND clear `bendPending` so a stolen mid-bend voice can't ghost-fire its transition after `allNotesOff`. | Per-phrase log: `inst N CX face=F slides=S bends=B leaps=L dur=D`. D42 `GLISS FAIL` invariant: `slides + bends + leaps < 1` triggers fail. Current C5/C6/C7 normally log `bends=N`; `slides` is for legacy `glissNote` fallback. `BEND CLIP` logs when an over-range interval falls back to `leapStep`. `BEND FAIL` logs when `bendStep` is asked to bend a pitch not in `inst.activeNotes` (defensive fall-through to `leapStep`). `bend race-fix` logs when `completeBend` runs inline before its scheduled task, indicating scheduling jitter exceeded the 5 ms margin. Dashboard: `/xk/midi/bendstep` OSC echo flows through relay → WS `midi_echo` (kind=`bendstep`) → `triangle.bendStep` + `rolling-score.bendStep`. |
| **Wild-gliss bend integrity** - C5's wild-gliss identity is the bend salvo itself: the active `phraseC5` must define `wildAccent`, call `glissStep(... WILD_GLISS_VEL, wildAccent, bendDur)` for every scheduled target, and avoid fresh companion noteOns while a pitchbend ramp is pending. A C5 companion may be created before the salvo, but it must be re-voiced only by bend/leap completions, not by target-side companion noteOns during `bendPending`. This guards two C5-only silent failures: an active-function `wildAccent` reference escaping from the legacy commented block (scheduled tasks abort, leaving straight anchors) and per-rebow companion noteOns landing during `bendPending` with louder straight attacks that mask the source bend. | `max/xk_swam.js` `phraseC5` computes `wildAccent` in the active function and falls back to `WILD_GLISS_BPA` if it is non-finite. `maybeGlissDoubleStop` refuses to create a companion while `inst.bendPending` is active; `completeBend` and `leapStep` own companion re-voicing after the main gliss target lands. `src/phrase-plan.ts` mirrors the same companion lifecycle so relay phrase-audit counts match the bridge. `test/max-bridge.test.ts` statically checks the active `phraseC5` body, and `test/phrase-plan.test.ts` asserts re-voiced in-range C5 companions. | `C5 WILD ACCENT FAIL inst N accent=X - falling back to 80` if C5 accent resolution breaks; `BEND COMPANION FAIL inst N C5 ... - suppressed companion noteOn during pitchbend ramp` if any future phrase code tries to fire a target companion during a bend. The existing phrase audit still raises `missing all planned bendsteps` if C5 tasks abort before `glissStep`. |
| **Gliss double-stop continuity + range** - C5/C6 gliss companions are audible dyad voices for the path, not visual-only anchor ghosts. A companion may only be created if a fixed interval keeps every planned main gliss pitch inside the rolling-score companion range. Once created, the companion is re-voiced at every hard bend/leap completion so SWAM does not drop the held dyad after the onset. Runtime range drift ends the companion loudly instead of drawing or sounding a pitch outside the contract. | `max/xk_swam.js` `doubleStopCompanionForRange` chooses only intervals safe for the planned span; `maybeGlissDoubleStop` stores `inst.glissCompanion`; `completeBend` and `leapStep` noteOff the old companion and noteOn the target companion after the main transition; `scheduleRelease` checks that companion revoice count kept pace with bend + leap transitions. `src/phrase-plan.ts` mirrors the range picker and re-voice events. Dashboard range clipping lives in `public/js/rolling-score.js` `_companionOffsetAt` / `_companionDyFromOffset`. | `DOUBLE STOP RANGE SKIP inst N CX span=A..B range=...` when no fixed interval can keep the path in range; `DOUBLE STOP RANGE FAIL ... - ending gliss companion` if runtime drift escapes the range; `DOUBLE STOP FAIL ... companion missing before release` or `revoices=R bends=B leaps=L transitions=T` if the dyad drops before natural release. Normal success logs `inst N CX doubleStop offset=O revoices=R bends=B leaps=L span=A..B`. Static coverage: `test/max-bridge.test.ts`, `test/phrase-plan.test.ts`, `test/dashboard-ui.test.ts`. |
| **Pitchbend MIDI channel alignment** — pitchbend status bytes MUST use the same one-based `MIDI_CH` convention as note-on, note-off, and CC status bytes. The bug this row guards: `emitPitchbend` used `0xE0 + MIDI_CH` while notes used `0x90 + (MIDI_CH - 1)`. With `MIDI_CH = 1`, note events reached SWAM channel 1 while pitchbend went to channel 2. Relay/dashboard telemetry still showed `/xk/midi/bendstep` and per-phrase `bends=N`, but SWAM heard no wheel movement and played straight target notes. | `max/xk_swam.js` defines `statusPitchbend(ch) { return 0xE0 + (ch - 1); }`; `emitPitchbend` emits through that helper; `assertPitchbendChannel(status)` checks the status against the expected pitchbend status for `MIDI_CH`; `test/max-bridge.test.ts` statically rejects the old `emitMidi(inst, 0xE0 + MIDI_CH, ...)` pattern. | `PITCHBEND CHANNEL FAIL status=S expected=E MIDI_CH=C noteOnStatus=N ccStatus=B` if a future edit computes a pitchbend status byte outside the note/CC channel convention. Normal play should never log this. |
| **Pitchbend range bridge↔preset alignment** (D64) — `max/xk_swam.js` `PITCHBEND_RANGE_SEMI` MUST EXACTLY MATCH the SWAM preset's Master Tuning → Pitchbend Range setting (saved in `xenakube_2.swam` / current preset). MIDI pitchbend is a 14-bit wheel position with NO semitone information; SWAM does the conversion using its preset value. Mismatch silently produces audible-bend ≠ visual-bend: bridge sends value14 expecting one scale, SWAM applies another, audible bend is over- or under-strength by the ratio. Severe mismatches (preset reverted to default ±2 vs bridge at ±24) are 12× weaker than intended → audible bend barely moves, then the `noteOff(source) + noteOn(target)` at end of bend fires the discrete jump audibly → "leaping" perception even when the bridge is bending correctly. **Breaks silently any time the SWAM preset changes without a paired bridge re-sync.** | Manual sync of the bridge constant against the preset's UI value. `PITCHBEND_RANGE_SEMI` is a paired tunable — change one, change the other. | `bang()` logs `=== BRIDGE PITCHBEND_RANGE_SEMI = ±N — verify this matches SWAM preset's Pitchbend Range ===` on every Max v8 reload. Visible in `[print xk_swam]`. Cross-check against the SWAM preset's actual Pitchbend Range setting every reload. If they ever disagree, the bridge constant must be updated and the v8 reloaded. (Runtime auto-detection would require a `vst~ pattr` read-back — deferred; the loud reminder is the practical guard.) |

(Add rows here when you formalize harmonics / tremolo / KS invariants. Same structure: *what* must hold, *where* it's enforced, *how* a failure surfaces in the log.)
