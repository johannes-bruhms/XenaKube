node_modules/
dist/
.claude/*
!.claude/agents/
*.env
.DS_Store

# External MCP tooling pulled in for Max patching; not part of this project
maxmsp-mcp/
MaxMSP-MCP-Server/
max/max_mcp*.js
max/package.json
max/package-lock.json

# Local TD projects + backup snapshots + screenshots (not part of the committed repo)
TD/
backups/
docs/*.png
docs/temp-screenshots/
XenaKube-from-mac/

# Local browser/test artifacts
.tmp-chrome-xk/
AUDIT_REPORT.txt

# Local neural model payloads; keep project patches/config separate from these assets
Rave_Models/

# Local interruption-layer footage (use generated placeholders in committed code)
media/
public/interruption/media/

# Local Ableton project — not part of the committed repo
ableton/

# Local audio assets in the Max folder (the patch is source; recordings are not)
max/*.wav
